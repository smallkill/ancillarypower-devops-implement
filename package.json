{
  "name": "backend-service",
  "version": "1.0.0",
  "description": "Minimal runnable backend (routers / services / controllers)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "test": "jest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format:check": "prettier . --check",
    "format:fix": "prettier . --write",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.js": [
      "prettier --check",
      "eslint"
    ]
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "eslint": "^10.0.1",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "lint-staged": "^16.2.7",
    "prettier": "3.8.1",
    "supertest": "^7.0.0"
  }
}
